[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ml_core"
version = "0.1.0"
dependencies = [] # Add ml-specific deps here like torch or transformers

[tool.setuptools.packages.find]
where = ["src", "tests*"]
